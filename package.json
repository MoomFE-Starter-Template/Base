{
  "name": "nuxt-base",
  "private": true,
  "type": "module",
  "author": "Wei Zhang <https://github.com/Zhang-Wei-666>",
  "homepage": "https://github.com/MoomFE-Starter-Template/Base",
  "scripts": {
    "build": "pnpm i && nuxt build",
    "dev": "pnpm i && nuxt dev",
    "generate": "pnpm i && nuxt generate",
    "preview": "pnpm build && nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "vue": "latest"
  },
  "devDependencies": {
    "nuxt": "^3.12.4"
  }
}
